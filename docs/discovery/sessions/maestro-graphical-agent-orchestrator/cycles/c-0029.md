---
schema-version: 1
cycle: c-0029
session: maestro-graphical-agent-orchestrator
started: 2026-08-22T12:39:03Z
completed: 2026-08-22T13:09:14Z
anchor-revision: 2026-08-20T19:52:31Z
anchor-status: unchanged
entry-state-digest: a045b88a8b05d07c9be03f4f2aa56650897a8daec128fb6cdd3eb5a7663741b3
exit-state-digest: 15b8821d093076c176d199062333bde5c1e0af564a0879d1bbd18f816a6f52f7
entry-root-map-digest: 912d3aed70e2941d603bf7ae22f146f99ff73b9925e2736886aea9e45c585a41
exit-root-map-digest: 5f0aca06475f70f67d67a75542185e08714d22a1d4c228d5a5530fcdf7d58304
entry-root-lexicon-digest: 996bd740e483473691d06862dd280b3ac5929e3c4dfbea7ac4ecf803307c5ed5
exit-root-lexicon-digest: 7e776bcfef5345bdf38a3763c32047ab69852c6c2cdcdf6b071a631ee0dfd7df
digest-tool: shasum -a 256
digest-status: verified
question-group-size: 12
questions-asked: 0+0
selection-source: user
traversal-mode: broad
promotion: not-ready
promotion-identity: none
domain-handoff: none
domain-handoff-status: none
domain-handoff-pending: 0 (none)
tracker-mode: remote
state-scope: full
outcome: CYCLE_ADVANCED
composition-report: discovery=available; create-ticket=available; interrogate=available; domain-mapping=available; spec=available
---

# Cycle c-0029

## Selection

Reinterpretation deterministically selected n-0010 under rule 1 because the
accepted ADR 0004 invalidated and retired Fleet Recap. The invocation's explicit
handoff directed the first action to n-0013's spinner verification and cmux
surface work, so selection was overridden to **n-0013 - Maestro's observable
surface inside cmux**, broad across its linked lifecycle and native-integration
nodes. n-0010 was retired rather than investigated.

## Reinterpretation

- n-0010 moved from fog `scouted` to fog `invalidated`; `Recap` and its Fleet
  referent are deprecated by the canonical domain artifacts.
- n-0000 was renamed and reframed as the Maestro observability plugin. Its
  promoted tracker item now carries an explicit divergence because Issue #1
  still describes the retired standalone orchestrator.
- Every other node was intact. The route retirements and current cmux branch
  remain supported by the durable decisions that created them.

## Questions and Answers

No questions were asked. The group closed at 0/12 because every material item
was empirical, an implementation-safety decision the loop owns, or already
settled by the user's native-first principle.

## Decisions

- Keep cmux's Copilot integration installed and consume only the behavior it
  proves.
- Keep Maestro's fail-open hook entry for the subagent tree and live-working
  fallback; cmux's native hooks did not replace it.
- Remove `preToolUse` from the intended posture. It retains observer-veto risk
  and did not produce a native `running` transition.
- Mirror `CMUX_COPILOT_HOOKS_DISABLED=1` as Maestro's full hook kill switch.
- Treat native restore and hook-store Session identity as incomplete until the
  measured defects are fixed and reverified.

## Evidence

- The custom sidebar rendered end to end after reload.
- A controlled fixture showed an animated braille spinner for a running row and
  a red `xmark` for a failed row. The c-0026 zero-width hiding defect is fixed.
- `cmux sidebar open maestro` produced a real, resizable Bonsplit pane whose
  hierarchy and rows appeared in the accessibility tree.
- `set-status` and `set-progress` rendered visibly. `log` remained stored and
  queryable but did not appear on the workspace card.
- A 30-second watcher sampled the native store 519 times while a tool call
  stayed active for ten seconds. All 16,089 lifecycle observations remained
  `idle`; no `running` transition occurred.
- The native store reached 31 records and earlier showed 13 records sharing one
  process and transcript. One row cannot safely mean one Session.
- The native restore affordance generated a Copilot invocation with a bare
  `-C`, failing with `option '-C <directory>' argument missing`.
- Public cmux source confirms the operator supplied every documented restore
  argument and narrows the defect to captured or reconstructed launch arguments.

## Lexicon Changes

Mirrored the canonical repository domain result already committed in
`CONTEXT.md`, ADR 0001, and accepted ADR 0004:

- deprecated `Fleet`, `Parked`, `Interrupted`, `Primary Agent`, `Worktree`, and
  `Recap`;
- restored `Session` as the runtime-owned structural unit Maestro observes;
- added confirmed `Host Application`;
- rewrote the confirmed domain mirror and both lexicons against those artifacts.

No new domain handoff ran.

## Promotion Outcome

`not-ready`. n-0013 remains fog `decision-ready` and maturity `researched`, with
the replacement tree channel, task-list presentation, and editor hand-off file
still unresolved. No preview or tracker mutation was attempted.

## State Delta

- Reconciled `requirements.md` from the retired orchestration and multi-route
  specification to the current observability-plugin contract.
- Rewrote the primary map around one cmux-hosted plugin and its bounded defects.
- Updated n-0000, n-0010, n-0011, n-0013, n-0014, and n-0016.
- Recorded the complete c-0029 prototype, visual, lifecycle, duplication, and
  restore evidence.
- Reopened Issue #1's tracker divergence without changing the tracker.

## Limitations

- `needsInput` and `unknown` were not exercised.
- The exact captured token that left `-C` without a value was not isolated.
- Duplicate records were grouped by process and transcript equality, but their
  identity-generation cause remains unknown.
- The tracker anchor remains stale; `/discovery` owns its reconciliation.

## Next Frontier

Advisory for c-0030: rule 6 returns to n-0013, the oldest P0 node at maturity
`researched` among the tied surface candidates. Resolve the replacement for the
transitional workspace-description wire format while preserving parentage,
state, and activity. No priority debt is open.
