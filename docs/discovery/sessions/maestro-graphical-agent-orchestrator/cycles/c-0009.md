---
schema-version: 1
cycle: c-0009
session: maestro-graphical-agent-orchestrator
started: 2026-08-19T16:09:38-04:00
completed: 2026-08-19T16:34:00-04:00
anchor-revision: 2026-08-19T08:47:53-04:00
anchor-status: unchanged
entry-state-digest: 42e182e80b394421842c99bc863a1e25bdb08841918635bcc4357bf2a0f6e78c
exit-state-digest: b1fa9ebff5fa9388266729b0a978f003a513b0dd0381f4f75e2996813b62a314
entry-root-map-digest: d3619f69cbe739cbf5c24bacb53c809a060a32a1980f21830bd8d6f072e6bb2d
exit-root-map-digest: d3619f69cbe739cbf5c24bacb53c809a060a32a1980f21830bd8d6f072e6bb2d
entry-root-lexicon-digest: 1315c0623a502cd61b8b0ec1a93a4979fb1c61b7d904be0e9d79dca3466bdba6
exit-root-lexicon-digest: 1315c0623a502cd61b8b0ec1a93a4979fb1c61b7d904be0e9d79dca3466bdba6
digest-tool: shasum -a 256
digest-status: verified
question-group-size: 12
questions-asked: 0+0
selection-source: deterministic
traversal-mode: deep
promotion: none
promotion-identity: none
domain-handoff: none
domain-handoff-status: none
domain-handoff-pending: 0 (none)
tracker-mode: remote
state-scope: full
prototype: n-0000, approved, run, cleaned up and verified
outcome: CYCLE_ADVANCED
composition-report: discovery=available; create-ticket=available; interrogate=available; domain-mapping=available; spec=available; ship-with-squadron=blocked (missing /handoff)
---

# Cycle c-0009

## Selection

`n-0000`, the only node, `selection-source: deterministic`, traversal deep.
Entry digests matched the c-0008 exit values. Node verdict `intact`.

Fog classification put exactly one open item in the prototype-only class, and it
was the node's single recorded blocker. No question group ran: the cycle's open
item was empirical, not a user decision, and the reference is explicit that user
attention is spent on decisions rather than on facts the loop can establish.

## Prototype

Approved in a live turn as `Approve prototype n-0000`, bound to
`.discovery-prototypes/maestro-graphical-agent-orchestrator/n-0000-c-0009/`.

**Hypothesis:** a supervisor can guarantee zero surviving descendants after
graceful quit, `SIGKILL`, and Force Quit, using non-detached spawn,
process-group termination, durable process-group recording, and reap-on-launch.

**Result: confirmed with one part of the hypothesis falsified.** The teardown
guarantee holds. The spawn strategy named in it does not.

### A recorded requirement was falsified by measurement

| Scenario | `detached` | Distinct process groups | Survivors | Verdict |
| --- | --- | --- | --- | --- |
| A1 graceful quit | `false` | 1 | 6 | FAIL |
| A2 SIGKILL, no reaper | `false` | 1 | 6 | FAIL |
| B1 graceful quit | `true` | 2 | 0 | PASS |
| B2 SIGKILL, no reaper | `true` | 2 | 6 | FAIL, expected |
| B3 SIGKILL then reap-on-launch | `true` | 2 | 0 | PASS |

c-0006 recorded, from research rather than experiment, that Maestro should
"spawn Agents non-detached" and that "`detached: true` is what causes
orphaning". Both A scenarios prove the opposite.

A non-detached child is **not a process-group leader**. It shares the
supervisor's group, so `process.kill(-pid)` returns `ESRCH`: there is no group
at that address to signal. Teardown does not degrade, it cannot be addressed at
all, and all six descendants survived even a graceful quit.

**`detached: true` is required**, not forbidden. It is what gives each Fleet its
own process group, and therefore what makes complete teardown of a nested tree,
and targeted per-Fleet cancellation, possible at all.

The correct statement of the c-0006 lesson is narrower than what was recorded:
detachment *without* durable ownership and a reaper causes orphans. B2 and B3
differ only by the reaper, and they differ by six surviving processes.

### Electron confirmation

Electron installed inside the isolation path in **8 seconds**, contradicting the
c-0001 and c-0005 premise that its download was a meaningful cost or a reason to
sequence the routes.

Against Electron's real main process and lifecycle events: `BrowserWindow`
rendered a three-column layout at 1200x700; three Fleets started as three
distinct process groups totalling nine processes; `before-quit` tore down all
three groups; `will-quit` verified **zero survivors**; reap-on-launch ran first
and correctly found nothing on a clean start. Zero stray processes remained.

### Cleanup

Removed exactly
`.discovery-prototypes/maestro-graphical-agent-orchestrator/n-0000-c-0009/` and
verified its absence. The parent directory is retained and empty. Zero
prototype processes remain. No file outside the isolation path was created or
modified by the prototype.

`.discovery-prototypes/` was added to `.gitignore` under the same approval,
before any prototype file was written. The directory already existed, untracked
and not ignored, so prototype scratch could have reached `main` through a stray
`git add -A`.

## State delta

- `requirements.md`: the non-detached requirement struck and replaced by two
  measured requirements - each Fleet spawns detached in its own process group,
  and detachment is safe only when paired with durable process-group ownership,
  verified escalating termination, and reap-on-launch.
- `evidence.md`: two c-0009 sections carrying the measurement tables.
- `discovery.md`: revision 10, node blockers reduced to **none**, open questions
  replaced with the two the prototype newly exposed, history extended.
- Root files unchanged this cycle; their digests are identical at entry and
  exit.

## Limitations

- The process trees were synthetic `sh` and `sleep` processes, not live Copilot
  sessions, which may install their own signal handlers. The property under test
  was process-tree shape, which is where c-0006 showed the failure occurs.
- Packaging was not tested; only the development-run lifecycle was exercised.
- macOS Force Quit was simulated with `SIGKILL`, the signal it sends, rather
  than triggered through the operating system interface.
- A prototype is disposable and is not production work. This cycle proved the
  riskiest property is achievable; building the product follows promotion
  through a delivery workflow.

## Carry-over

Packaging versus development-run lifecycle; live Copilot signal handling;
sibling awareness; the Attention rule replacing `AT_RISK`; subagent tree depth;
the 8-Fleet ceiling under worktree isolation in a monorepo; whether `herdr`
remains in the architecture; and the unexamined inter-session messaging surface.
