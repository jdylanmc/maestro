---
schema-version: 1
cycle: c-0020
session: maestro-graphical-agent-orchestrator
started: 2026-08-20T22:08:43Z
completed: 2026-08-20T22:41:00Z
anchor-revision: 2026-08-20T19:52:31Z
anchor-status: revised
entry-state-digest: b40e640706ab20423f3171f6b6d2ce6204906b1b98b4851afac9de8e1201d218
exit-state-digest: 09af61279fd102341d00379c3981f0d10079a62284197568a95e3665963526ca
entry-root-map-digest: 337ebf920d2bb4c4b3401c57f86bdae931717f703f5d1372697969d21664b28d
exit-root-map-digest: 6268316720e4d05f1c329ee039d6ec8dab3c29964692c38f228d8109e5f135c8
entry-root-lexicon-digest: 996bd740e483473691d06862dd280b3ac5929e3c4dfbea7ac4ecf803307c5ed5
exit-root-lexicon-digest: 996bd740e483473691d06862dd280b3ac5929e3c4dfbea7ac4ecf803307c5ed5
digest-tool: shasum -a 256
digest-status: verified
question-group-size: 12
questions-asked: 2+1
selection-source: deterministic
traversal-mode: deep
promotion: not-ready
domain-handoff: none
domain-handoff-status: none
domain-handoff-pending: 0 (none)
tracker-mode: remote
state-scope: full
outcome: CYCLE_ADVANCED
composition-report: discovery=available; create-ticket=available; interrogate=available; domain-mapping=available; spec=available
---

# Cycle c-0020

## Selection

**Selected: n-0008 - Copilot integration mode. `selection-source: deterministic`, traversal `deep`.**

Rule 3 (shared blocker), **fourth consecutive cycle**. Candidate set was n-0004 (2 dependents, P1),
n-0005 (2, P1), and n-0008 (3, P0); the priority tie-break resolved to n-0008 outright. The three
previous cycles deferred it. This one took it, and it turned out to be the cheapest node in the
session rather than the most expensive.

## Reinterpretation

**All eleven nodes `intact`.** The anchor was `revised` by timestamp - observed
2026-08-20T19:52:31Z against a recorded 2026-08-20T17:18:25Z - but the movement is **this loop's
own c-0019 promotion write plus sub-issue and close events**. The body was read in full and every
reconciled clause is present and unchanged. No substantive clause moved, so no verdict could
differ on anchor grounds.

## Questions and Answers

**Q1 (grounded) - Is the subagent tree a live view or a reconstruction from durable state?**
Recommended live-first with durable fallback, justified partly by the `ephemeral` completeness gap.
**Answer:** live-first with durable fallback.

**Q2 (follow-up) - Does that answer stand once its premise weakens?** Asked because the loop's own
measurement - 0 ephemeral events in 36,517 - had just deflated the completeness argument used to
justify Q1's recommendation. **The framing failed:** *"Again, treat me like the product manager. I
have not a clue what you are talking about."*

**Q3 (grounded; Q2 re-put in product language) - Should helpers appear in the tree the moment they
start, or fill in when you next look at that Fleet?** **Answer:** *"Live - helpers pop into the tree
the moment they start"*. `settled`.

**3 of up to 12 asked, 9 unused.** Closed on the second stop condition.

## Decisions

- **The subagent tree updates live.** Maestro subscribes while a Fleet is `Alive` and reconstructs
  from durable state when it is `Parked` or `Dead`; the Acceptance Harness asserts both paths.
  Owner: user. Updates n-0009, informs n-0010.
- **The live-first decision now rests on immediacy, not on completeness.** The justification
  changed even though the choice did not, because measurement undermined the original argument
  before the user ever saw it. Recorded because the harness must assert the former.
- **n-0008 is cleared** on first-party evidence, with one quota-gated accepted unknown remaining.
  Owner: loop, on evidence.

## Evidence

**The prototype classification was wrong, and it had been wrong for six cycles.** c-0019 recorded
all three of n-0008's questions as prototype-gated, one explicitly as needing "an `npm install` in
an isolation path, **not** a free read". The SDK is already on disk with full typings in five
installed versions under `~/.copilot/pkg/darwin-arm64/`. Two of the three were repository-fact
class all along. **A cost was asserted rather than checked, then carried forward unexamined** - the
same shape as c-0014, where a seam was chosen without looking at a third option sitting in the same
package.

From the typings:

- **`subagent.*` are typed SDK events** - `started`, `completed`, `failed`, `selected`,
  `deselected` - and `EventsAgentScope` `'primary'` returns "main-agent events plus events whose
  type starts with `subagent.`". The State Oracle need not parse `events.jsonl` for the tree.
- **`parentId` is excluded by the vendor's own words**: "ID of the chronologically preceding event
  in the session, forming a linked chain." c-0010 inferred this from measurement; the runtime's
  published types now say it outright.
- **No rename API**, but `SessionConfig.sessionId` is caller-suppliable, so Maestro can *mint* the
  identifier it binds to. This strengthens the c-0013 decision rather than reopening it.
- **Version bound**: the four load-bearing surfaces are identically shaped across **1.0.80 through
  1.0.81-5** - the evidence `docs/adr/0002` has lacked since c-0016.

From 36,517 real events across two previously unmeasured sessions: **0 ephemeral**; `agentId` ==
`data.toolCallId` on all 85 `subagent.started`; distinct `agentId` count exactly equal to the
started count (15/15, 70/70); and the c-0010 tree join **resolving 85 of 85 with zero unresolved**,
at depths 1 and 2 with fan-out dominating.

**That last one is a revalidation, not a discovery**, and the checkpoint says so because the loop's
first draft of this cycle claimed otherwise. c-0010 had already specified the join and
`requirements.md` already recorded it. What changed is its evidence class: it now holds on data it
was not derived from, and it is independently corroborated by first-party documentation.

## Lexicon Changes

**None.** No new language appeared in the anchor or the answers. The four `candidate` terms -
`Acceptance Slice`, `Acceptance Harness`, `State Oracle`, `Presentation Check` - had `Last verified`
refreshed to c-0020, with the c-0019 reason standing: their bounded context is
`Maestro discovery process`, not the product, so the product glossary is not their home. Zero
`conflicted` terms in either lexicon. `domain-handoff-status: none`.

## Promotion Outcome

**`not-ready`.** n-0008 reaches fog `cleared` but maturity `decision-ready`, one level below the
promotion floor - and it is provider understanding rather than deliverable work, so it has no
natural tier. No other node changed. The two published Stories are untouched.

## State Delta

- n-0008: fog `decision-ready` -> `cleared`; maturity `researched` -> `decision-ready`; two of
  three open questions answered and reclassified; leaves the Active Frontier.
- n-0009: evidence and history extended - slice step 3 de-risked and the live-tree requirement
  attached. Fog unchanged at `promoted`.
- n-0010: first open question narrowed - the raw material is now known to be *available*; whether
  it is *sufficient* is still open.
- `requirements.md`: **the c-0011 "all four routes are driven to a complete MVP" bullet is struck
  through**, two cycles after c-0018 superseded it. It had contradicted its own section since then
  and was carried as a known defect.
- `requirements.md`: six new confirmed requirements under `Runtime integration`.
- Priority Debt: still empty. n-0008 advanced *upward*, which can only close rows.

## Limitations

- **The one remaining item on n-0008 is untouched**: the SDK permission callback has still never
  been observed firing. Quota-gated, unchanged since c-0014.
- **`ephemeral` = 0 bounds an observed frequency; it does not prove the field is never set.**
  *Trigger:* the first ephemeral event ever observed.
- Both measured sessions are Copilot CLI sessions on this machine. Nothing here establishes
  behaviour under a differently configured runtime.
- **ADR 0002 still lacks its version bound.** The blocking evidence is now gathered, but the edit
  is `/domain-mapping`'s to make. Fourth cycle disclosed.
- Node `History` fields still exceed the schema's length bound. Fifth cycle disclosed.
- **The WezTerm Presentation Check ceiling is still research-derived**, feeding a fixed rubric
  criterion for a committed route. Permission to measure it has been unused since c-0018.
- **The loop mis-framed a question to the user for the second time this session.** Both times the
  user had to redirect it toward product language. Recorded as a loop defect, not a user hesitation.

## Next Frontier

**Deterministically, rule 3 now reaches n-0004 (v1.1 WezTerm)** - with n-0008 cleared, the
remaining shared blockers are n-0004 and n-0005, both P1 with two dependents, and n-0004 wins on
first-seen order. **But the advisory recommendation is unchanged from c-0019 and is now stronger:
build, do not clear fog.**

n-0004 must not be deepened yet. The confirmed requirement is that routes run **strictly one at a
time**, and the Electron route has not started. Deepening WezTerm now would also let the shared
Acceptance Harness be shaped by two routes at once, which is the exact bias the State Oracle exists
to prevent.

Ordered path:

1. Build [#30](https://github.com/jdylanmc/maestro/issues/30) - and **step 3 is now the
   best-specified step in the harness**, with a construction validated at 100% on 85 subagents and
   a live-subscription path the user has confirmed is required.
2. Build [#29](https://github.com/jdylanmc/maestro/issues/29) against it.
3. Return to the loop for n-0004 only after the Electron route produces its executive report.

Cheapest remaining evidence upgrades: measure the WezTerm ceiling directly (permission held since
c-0018), and close [#10](https://github.com/jdylanmc/maestro/issues/10) through `/discovery`, whose
last doubt this cycle removed.
